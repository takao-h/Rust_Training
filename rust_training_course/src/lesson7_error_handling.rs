// ここに `parse_and_add` 関数を実装してください。

use std::num::ParseIntError;

pub fn parse_and_add(s1: &str, s2: &str) -> Result<i32, ParseIntError> {
    // ここを実装してください
    // s1とs2をi32にパースし、その和を返します。
    // パースに失敗した場合は、そのエラーを返します。
    Err("Not implemented".parse::<i32>().unwrap_err())
}
